exports.deploy = (model, serviceName, options) => {
  const hanaDeployer = require('./hana');

  return hanaDeployer.deploy({
    ...options,
    model,
    serviceName,
    tunnelAddress: options['tunnel-address'],
    vcapFile: options['vcap-file'],
    hdiOptions: {
      autoUndeploy: options['auto-undeploy'],
      parameter: {"com.sap.hana.di.table/try_fast_table_migration": "true" }
    }
  });
}
