const { eventMesh } = require('../../registries/mta')

module.exports = class EventMeshSharedTemplate extends require('../enterprise-messaging-shared') {
  constructor(...args) {
    super(...args)
    this._name = 'event-mesh-shared'
    this._kind = 'enterprise-messaging-amqp'
    this._vcap = 'event-mesh-message-client'
    this._dirname = __dirname
    this._mta = eventMesh
  }

  static help() {
    return 'messaging via shared Event Mesh of SAP Integration Suite'
  }
}
