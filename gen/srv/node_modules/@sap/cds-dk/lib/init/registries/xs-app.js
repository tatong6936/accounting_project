const srvApi = {
  in: 'routes',
  where: { destination: 'srv-api' }
}

const html5RepoApi = {
  in: 'routes',
  where: { service: 'html5-apps-repo-rt' }
}

module.exports = {
  srvApi,
  html5RepoApi
}
